# Raw source media archive — do NOT commit (200MB+, contains a 155MB video over GitHub's limit).
# Only optimized copies under images/ and selected docs are committed.
/OUR WORKS/
_extract.txt
google-apps-script/Code.gs

# Private / sensitive — kept local only (legal docs: Trust Deed, PAN/FORM 10AC, GST RC,
# registration cert, member details, letterpads) and raw event source photos.
images/filess/
images/events/
